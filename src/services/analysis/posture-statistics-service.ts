import {calculatePoseSymmetry} from "../../lib/analysis/pose-calculations";import type{FramePoseResult,PostureStatistics}from"../../types/ai-analysis";
const vals=(f:FramePoseResult[],k:keyof NonNullable<FramePoseResult["jointAngles"]>)=>f.map(x=>x.jointAngles?.[k]).filter((x):x is number=>Number.isFinite(x));const mean=(a:number[])=>a.length?a.reduce((x,y)=>x+y,0)/a.length:undefined;const std=(a:number[])=>{const m=mean(a);return m===undefined?undefined:Math.sqrt(a.reduce((s,x)=>s+(x-m)**2,0)/a.length)};
export function calculatePostureStatistics(frames:FramePoseResult[]):PostureStatistics{const valid=frames.filter(f=>f.detected),torso=vals(valid,"torsoInclination"),sym=valid.map(f=>f.jointAngles?calculatePoseSymmetry(f.jointAngles):undefined).filter((x):x is number=>x!==undefined),variation=std(torso);return{averageTorsoInclination:mean(torso),torsoVariation:variation,averageLeftKneeAngle:mean(vals(valid,"leftKnee")),averageRightKneeAngle:mean(vals(valid,"rightKnee")),averageLeftElbowAngle:mean(vals(valid,"leftElbow")),averageRightElbowAngle:mean(vals(valid,"rightElbow")),symmetryScore:mean(sym),bodyStabilityScore:valid.length>=20&&variation!==undefined?Math.max(0,Math.min(100,Math.round(100-variation*3))):undefined};}
