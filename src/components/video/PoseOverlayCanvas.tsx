import React,{useEffect,useRef}from"react";import type{FramePoseResult}from"../../types/ai-analysis";
const links=[[11,13],[13,15],[12,14],[14,16],[11,12],[11,23],[12,24],[23,24],[23,25],[25,27],[24,26],[26,28]];
export default function PoseOverlayCanvas({frame,showAngles=true}:{frame?:FramePoseResult;showAngles?:boolean}){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current,ctx=c?.getContext("2d");if(!c||!ctx)return;const rect=c.getBoundingClientRect(),dpr=devicePixelRatio||1;c.width=rect.width*dpr;c.height=rect.height*dpr;ctx.scale(dpr,dpr);ctx.clearRect(0,0,rect.width,rect.height);const p=frame?.landmarks;if(!p)return;ctx.strokeStyle="#00F0FF";ctx.lineWidth=2;for(const[a,b]of links){if((p[a]?.visibility??0)>.45&&(p[b]?.visibility??0)>.45){ctx.beginPath();ctx.moveTo(p[a].x*rect.width,p[a].y*rect.height);ctx.lineTo(p[b].x*rect.width,p[b].y*rect.height);ctx.stroke()}}ctx.fillStyle="#00FF87";p.forEach(x=>{if((x.visibility??0)>.45){ctx.beginPath();ctx.arc(x.x*rect.width,x.y*rect.height,3,0,Math.PI*2);ctx.fill()}});if(showAngles&&frame?.jointAngles){ctx.fillStyle="#fff";ctx.font="11px monospace";ctx.fillText(`Tronc ${frame.jointAngles.torsoInclination??"—"}°`,12,20);ctx.fillText(`Confiance ${Math.round(frame.confidence*100)}%`,12,36)}},[frame,showAngles]);return <canvas ref={ref} className="pointer-events-none absolute inset-0 h-full w-full" aria-label="Squelette MediaPipe"/>}
